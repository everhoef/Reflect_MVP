package direct.reflect.facilitator.configurator;

public enum LaneType {
    MULTI_TEXT,    // Multiple sticky notes (Mad Sad Glad style)
    SINGLE_CHOICE  // Single selection from options (Feedback Door style)
}